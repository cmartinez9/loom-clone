# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
coverage/

# Build output
.next/
out/
build/
dist/

# ...except electron-builder's buildResources directory, which holds exactly one
# tracked file: the entitlements the hardened-runtime app is signed against.
!/build/
/build/*
!/build/entitlements.mac.plist

# Production
*.tsbuildinfo
next-env.d.ts

# Environment
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Editors / OS
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code
.claude/settings.local.json

# Electron build output
dist/
release/

# no-mistakes
.no-mistakes/

# Screenshots from scripts/screenshot.cjs
shots/

# The signed-bundle permission report (scripts/verify-permissions.mjs). A machine
# and a moment in time, not a fact about the code — the obligations it closes are
# recorded in AGENTS.md instead.
verify-permissions.json
